import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Yanis Portfolio",
    template: "%s | Yanis Portfolio",
  },
  description: "Senior Full Stack Dev & UI/UX Enthusiast",
  metadataBase: new URL("https://yanis-harrat.com"),
  openGraph: {
    title: "Yanis Portfolio",
    description: "Senior Full Stack Dev & UI/UX Enthusiast",
    url: "https://yanis-harrat.com",
    siteName: "Yanis Portfolio",
    locale: "fr_FR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  twitter: {
    title: "Yanis Portfolio",
    card: "summary_large_image",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
